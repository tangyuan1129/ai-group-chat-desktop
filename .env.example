# 把这个文件复制一份，改名为 .env，然后填入你自己的密钥
# （程序启动时会读取同目录下的 .env）

# [必填] 智谱 GLM 的 API Key —— 在 https://open.bigmodel.cn 或 https://api.z.ai 申请
ZAI_API_KEY=你的智谱密钥

# [可选] GitHub Token —— 想让工程师角色调用 GitHub 写操作时填写
#（不填也能用公开查询；个人访问令牌在 GitHub 设置里生成）
GITHUB_TOKEN=你的GitHub令牌